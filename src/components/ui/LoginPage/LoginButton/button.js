import React from 'react';
import './button.css';

const LoginDefaultButton = ({children}) => {
    
    return(
        <button className='LoginDefaultButton'>{children}</button>
    )

};

export default LoginDefaultButton;